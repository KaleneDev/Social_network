const express = require('express');
const router = express.Router();
const relationCtrl = require('../controllers/follow.controller');

// router.get('/', relationCtrl.getAll);
// router.post('/', relationCtrl.followUser);
// router.get('/:id', relationCtrl.getOne);
// router.put('/:id', relationCtrl.update);
// router.delete('/', relationCtrl.unfollowUser);

module.exports = router;